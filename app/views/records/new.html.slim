h1 入出金記録の追加
= link_to '戻る', book_records_path, class: 'btn btn-info mb-5'

=form_with model: [@book, @record], local: true do |f|
  .form-group
    =f.label :date, "日付"
    =f.date_select :date, default: Date.today, class: 'form-control'
  .form-group
    =f.label :content, "内容"
    =f.text_field :content, class: 'form-control'
  .form-group
    =f.label :amount, "金額"
    =f.text_field :amount, class: 'form-control'
  .form-group
    =f.label :wallet, "支払者"
    =f.text_field :wallet, class: 'form-control'
  .text-center
    =f.submit class: 'btn btn-info'